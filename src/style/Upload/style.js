import styled from "styled-components";
import { FieldSet } from "../../pages/Register/style";

export const Root = styled(FieldSet)`
  border: none;
`;

export const Input = styled.input``;

export const Upload = {
  Root,
  Input,
};
